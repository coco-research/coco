# /SI-GTM — Sales, GTM & Marketing Super Intelligence Team front door

> Type plain English after `/SI-GTM` and this team answers. The dispatcher infers the verb and the dials — users never need the scoped sub-commands unless they want them. Generated by `build_commands.py`.

## Step 0 — Read registry

Read `/Users/rijulkalra/code/coco/systems/superintelligence/gtm/registry.json`. If missing, tell the user to run `build_registry.py` and STOP.

## Step 1 — No args? Short roster, not a command dump

Print: team name, 26 personas across 7 cells (derive per-cell counts + 1-2 example names live from the registry), three example prompts, and the one-line rule: *"just type /SI-GTM followed by your question."*

## Step 2 — Free text: infer the VERB (same table as /SI, first match wins)

choose between options → Decide · yes/no tally → Vote · compare on dimensions → Tradeoff · critique a doc/PR/design → Review · what could go wrong (before) → Pre-Mortem · why did it fail (after) → Post-Mortem · attack it → Stress-Test · be brutal → Roast · plan it → Plan · architect it → Design · diagnose → Debug · steelman → Defend · new evidence on a prior take → Re-Analyse · give me a read → Analyse · whole thing end-to-end → Full-Cycle (warn ~5x latency).

Two verbs genuinely fit → ONE `AskUserQuestion` with the top-2. Truly generic → Analyse.

## Step 3 — Infer the DIALS from the wording

"make them argue / debate it" → `--debate` · "highest stakes / irreversible" → suggest `--debate=full` (confirm — expensive) · "in depth / full detail" → `--deep`. Explicit flags always win over inference.

## Step 4 — Run it

Invoke `/SI-GTM-<Verb> "<prompt>" <dials>`. One-line confirm before the panel runs:
> `<Verb> · GTM · <N> experts<, debating>` — reply 'edit' to change, anything else proceeds.

## Power-user shortcuts (mention only when asked)

- Direct verbs: `/SI-GTM-Decide` · `-Analyse` · `-Review` · `-Re-Analyse` · `-Pre-Mortem` · `-Post-Mortem` · `-Full-Cycle` · `-Tradeoff` · `-Plan` · `-Design` · `-Vote` · `-Debug` · `-Stress-Test` · `-Defend` · `-Roast`
- Identity: `/SI-GTM-Ask <slug> "<q>"` · `/SI-GTM-Huddle <cell> "<topic>"` · `/SI-GTM-Meeting "<prompt>"` · `/SI-GTM-Read <slug>`
- Roster/maintenance: `/SI-GTM-Recruit` · `-Refresh` · `-VoiceCheck` · `-Verify` · `-Orchestrate` (selection only — verbs call it)
- Flags: `--deep` · `--debate` / `--debate=full` · `--no-orchestrate` · `--cells` · `--personas` · `--coco-off`

ARGUMENTS: {{ARGUMENTS}}
