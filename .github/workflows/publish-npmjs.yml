name: Publish to npmjs.com

# Publishes cocosuperintelligence to the public npmjs.com registry.
# Frictionless install for users — no auth required: `npm install -g cocosuperintelligence`.
#
# Setup (one-time):
#   1. Create npm account at https://www.npmjs.com/signup
#   2. Generate token: https://www.npmjs.com/settings/<username>/tokens (type: Automation)
#   3. Add as repo secret named NPM_TOKEN: Settings → Secrets and variables → Actions → New
#   4. Trigger this workflow on a release or manually

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          scope: '@coco-research'

      - name: Verify package.json
        run: |
          node -e "
          const pkg = require('./package.json');
          if (!pkg.name.startsWith('@coco-research/')) {
            console.error('package.json name must be scoped @coco-research/*');
            process.exit(1);
          }
          console.log('package.json valid:', pkg.name, pkg.version);
          "

      - name: Confirm bin/coco.js syntax
        run: node --check bin/coco.js

      - name: Publish
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
