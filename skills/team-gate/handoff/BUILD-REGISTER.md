# BUILD-REGISTER — /team:* enforcement retrofit

Owner: Fable 5.1 (brain). Builder: Haiku 4.5. Verifier: Sonnet 5. Opus 5 only with the owner's permission. No task is DONE on the builder's word; DONE requires a verifier verdict, with the command and exit code recorded in the Evidence column.

| # | Task | Builder | Verifier verdict | Evidence | Status |
|---|---|---|---|---|---|
| 0 | Worktree feat/team-gate-retrofit off origin/main 8fac7c0 | brain | n/a | `git worktree list` | DONE |
| 1 | Fix relative arch-index invocations in command files (13, not 14: toolkit.md:164 is prose) | Haiku | Sonnet PASS: old form absent (grep exit 1), 13 new-form hits across 5 files, old form fails from /tmp, new form exits 0 from /tmp, diff --check clean, roles.md untouched | commit on feat/team-gate-retrofit | DONE |
| 1b | Follow-up: rewrite remaining relative references in toolkit.md:164, skills/arch-index/SKILL.md, .arch/ARCH-EVIDENCE.md, .arch/INDEX.md | brain | DONE 8a5d105 | toolkit.md prose reference and the two SKILL.md invocations now use the installed path; run_fixtures.sh example left relative (repository-only command); .arch files are generated outputs and were not hand-edited; old form grep exit 1; both installed scripts answer --help from /tmp | DONE |
| 2 | Add .team-ship/ to coco .gitignore | Haiku | Sonnet PASS: `git check-ignore -v .team-ship/` matches .gitignore:47 (note: needs trailing slash, directory-only pattern) | same commit | DONE |
| 3 | Pilot-day assumption 3: agent_id present on subagent tool calls | probe hook | brain read events.jsonl: 2 distinct agent_id values, agent_type general-purpose on the builder's calls | `~/.team/probe/events.jsonl`, 9+ rows, PreToolUse and PostToolUse both carry agent_id, agent_type, permission_mode, tool_use_id, transcript_path | DONE |
| 4 | Payload shapes for Stop, UserPromptSubmit, SubagentStop | probe hook | brain read events.jsonl: 1 Stop, 2 SubagentStop, 1 UserPromptSubmit captured | SubagentStop: agent_id, agent_transcript_path, agent_type, last_assistant_message, permission_mode, background_tasks, stop_hook_active. Stop: last_assistant_message, transcript_path, background_tasks, stop_hook_active, effort. UserPromptSubmit: prompt (raw text), prompt_id, transcript_path. | DONE |
| 5 | gate_state.py shared library and receipt schema | Haiku | Sonnet BLOCK round 1: (a) clean removal of the last receipt verifies as intact, exit 0, because nothing records the expected count; (b) CLI main() collapses verify_chain's three states to 0/1, so unrunnable exits 1; self-test bypassed main() and never caught it. MAJOR: build_rounds counts every gate-result; append_receipt sys.exits inside a library; CLI cannot pass hook_payload; fixtures are runtime-generated not static. Round 2 built: head.json plus orphan-gate cross-reference, ChainResult three-state, subprocess self-test 7/7, InvalidReceiptKind, --hook-payload, static deterministic fixtures, fourteen kinds. Builder's own report says InvalidReceiptKind maps to exit 1, which contradicts the exit-2 contract; flagged to Sonnet as check C. Round 2 Sonnet BLOCK: both round-1 BLOCKs fixed and reproduced (tail-drop, head missing, orphan gate each exit 1; four CLI exit paths correct; self-test is subprocess-based). Regression as predicted: InvalidReceiptKind exits 1 not 2. MAJOR: orphan detection compares bare name to "gates/"-prefixed values, blames the recorded file, misses the real orphan; exit code right by accident. MAJOR: status exits 0 when blocked. Fixture determinism, head.json atomicity, fourteen kinds, --hook-payload, build_rounds all confirmed. Round 3 built: bogus-kind exit 2, orphan basename normalisation with a fixture asserting stderr names 9.json, status three-valued; self-test 11/11 now asserts stderr substrings. Sonnet round 3 re-verifying, with a new concurrent-append adversarial (eight processes, forty appends) since parallel Layer-2 hooks will contend on receipts.jsonl and append_receipt may hold no lock. Round 3 Sonnet PASS: all regression holds, three fixes confirmed live through the CLI. MAJOR: no lock on append_receipt; eight writers, forty appends, seq reached 21 with duplicates, head.json undercounted; verify_chain fails safe. MINOR: nested gate paths out of contract, caveat needed. Committed f3f028c as verified for sequential use. Round 4 (fcntl lock, concurrent self-test, schema caveat) in progress; must land before task 14 hooks append concurrently. | verify-gate-state round-3 report; commit f3f028c | DONE: 898569d (lock), 4b5098c (two-way verification, containment, corrupt-tail refusal, raw-byte hashes), 8fb07bd (latest receipt wins, task 5d); follow-up 5c CLOSED (find_run raises RunMarkerUnreadable on a marker it cannot read, and every importer converts that to its own exit-2 path; 19 CLI probes, one clean ERROR line each, no traceback) |
| 5e | Follow-up: ship_gate._resolve and brownfield_map._resolve_context resolve a relative run.json repo_root against cwd, gate_state.verify_chain against the run directory; start_run always writes absolute, so no live effect; align on the run directory when either script is next opened | grok builder | adversarial verify BLOCK round 1: single-segment paths agreed, the malformed class did not, because verify_chain refuses a relative value containing a slash or equal to '.' or '..' while the aggregators joined it. Closing round 2 | The rule now lives once, in gate_state: recorded_repo_root_malformed plus resolve_recorded_repo_root. An absolute path resolves as-is, a single relative segment joins the run directory, anything else refuses with `run.json repo_root malformed: '<value>'`. Probe matrix over `../other`, `.`, `..`, `../../../../nope`, `sub/dir`, a single segment and an absolute path: ship_gate, fix_gate, brownfield_map and verify-chain agree on the class for every value. brain re-probed independently: exit 2, no traceback, the malformed value named. relative-repo-root self-tests still pass in both ship_gate and brownfield_map | DONE |
| 6 | arch_gate.py three-valued wrapper (closes D6) | Haiku | builder's own self-test 3 of 4 FAIL, reported honestly: make_fixtures.sh commits after writing pinned-commit so every pin is one behind HEAD and reads UNVERIFIED; D6 proof not produced; 196 files means four nested .git dirs that cannot be committed. Round 2 built (181 tool uses, 33 min): claims 4/4 PASS and drift_exit 0 / gate_exit 1 on remove-verdict. Report omitted the required verbatim outputs and git status. Brain's passive check found four nested .git directories still present outside _build/, contradicting the report's "no .git directories committed." Sonnet verifying with that as an explicit check. Round 2 Sonnet BLOCK: four stray .git dirs (clean/ would commit as gitlink), self-test in-process, validate launch failure misreported as exit 1, REMOVE reason never shown, make_fixtures not idempotent. Round 3 built with prescriptive brief in 17 tool uses (versus 181): stray dirs deleted, subprocess self-test, launch failure exit 2, reason leads with REMOVE, rm -rf per fixture. Sonnet round 3 re-verifying. | verify-arch-gate round-2 report; builder round-3 report | DONE: a5298a1 (three-valued wrapper, closes D6) and d4ea152 (task 6b portability for the CI runner) |
| 7 | check_artifacts.py and ship-manifest.json | Haiku (build-check-artifacts) | pending | brief: WAVE2-BRIEFS.md Task 7; adversarial: filler padded to minLines with no required heading must exit 1 | DONE, commit 0f9bb42; Sonnet rebuild, Sonnet verify PASS, four closing items brain-checked |
| 8 | run_gate.py (skip is failure, writes its own exit code) | Haiku (build-run-gate) | Builder's first report mapped skip to exit 2; brain sent a fix before any verifier; second report confirms exit 1 with skipped_as_failure field but again omitted verbatim self-test output. Sonnet verify-run-gate running with A3 (shell=True) emphasised. | builder reports x2 | DONE, commit 3854741; Sonnet rebuild, two Sonnet verifies, closing items brain-checked |
| 9 | prove_red.py (three-way classifier, detached worktree, AST hash) | Haiku (build-prove-red) | First report listed seven fixtures with expected exits as if tested. Brain demanded verbatim output: --self-test NOT IMPLEMENTED (argparse error), prove fails "run directory not found" on every fixture. Nothing was ever exercised. Round 4 brief sent: two-commit fixtures with BASE_SHA, self-test starts a run per fixture via gate_state.start_run, subprocess CLI with exit and stderr assertions, worktree-leak check. Sonnet not yet engaged. | builder verbatim run 3 | DONE, commit 0838729; Sonnet build in one round, Sonnet verify PASS |
| 10 | verify_independent.py (registered clean worktree, .team-ship absent) | Haiku (build-verify-independent) | pending | brief: Task 10; adversarial: team-ship-tracked names the tracked file, exits 1 | DONE, commit 516d68d after five Sonnet rounds; end-to-end use waits on run_gate --role verifier (task 8) |
| 11 | claim_evidence.py and the three render_* scripts | Haiku (build-claim-evidence) | pending | brief: Task 11; adversarial: "759 passed" plus "all tests pass" against 759 passed 4 skipped exits 1 | DONE, commit 1f04140; Sonnet rebuild, Sonnet verify PASS, six closing items brain-checked |
| 12 | ship_gate.py and fix_gate.py aggregators | Haiku | pending | fixtures | DONE: 12a f361d8d, 12b 8f1053d |
| 13 | run_fixtures.sh joins the CI step at ci.yml:187-189 | Haiku | pending | CI green on the fixture suite | DONE, commit 371e19e; clean-checkout proof 12/12 at 996a8e7; clean-checkout proof rerun at 0fa5e36: 14/14 (brownfield_map.py and hooks_selftest.py added); at 8b5655d: 14/14 after the hooks_selftest seed fix; at 53f3ea9 (with task 34): 14/14; at 45e5dd2 (every script change on the branch): 14/14 |
| 14 | Four hooks in observe mode plus lib.js, install.py, fixtures, hooks_selftest.py | Sonnet builds (rounds 1 to 3b), Sonnet verifies (round 1 BLOCK on the approval matcher, round 2 PASS: 14 named prompts plus 7 adversarial with no false positive, deny and stop forms byte-exact, certified pairing and gate-timeout live, receipt sha256 matched with shasum, 48 self-test cases OK) | DONE b88fa2f | hooks_selftest.py 48 OK; observe mode records would-deny and writes nothing; turn log writes receipts in both modes | DONE |
| 15 | Pilot-day assumptions 1, 2, 5 (deny inside subagent, timeout behaviour, Stop resume) | brain, isolated rig | Assumption 2 RESOLVED: harness FAILS OPEN on hook timeout. slow-deny.sh started 22:38:59Z, killed at 5s (no 20s line), marker.txt present, tool call ran. Assumption 1: not testable from an auto-mode session, documentation-confirmed only. Assumption 5 RESOLVED: a command Stop hook returning decision block resumes the model. Fired at 22:42:05 with stop_hook_active false, block emitted; model ran the required command; fired again at 22:44:58 with stop_hook_active true, allowed; resumed.txt present; claude exit 0; wall 712s. stop_hook_active flips to true on re-entry, so no counter is needed to prevent a loop. Assumption 4 RESOLVED: a parent-session tool_use_id from the probe log appears 6 times in the session transcript; a subagent tool_use_id appears 0 times in the parent transcript, as expected, and lives in the agent_transcript_path that SubagentStop supplies. | hooktest/results/hook-trace.log, marker.txt; hooktest/results-test5/hook-trace.log, resumed.txt; ~/.team/probe/events.jsonl cross-checked against the session JSONL | DONE: 2, 3, 4, 5 empirical; 1 documentation-only (not testable from an auto-mode session) |
| 16 | Deny 1: PR-open gate with recorded override | brain, live session 2026-09-18 | no code change; the run is the evidence | three `gh pr create` attempts each denied PreToolUse with `is_error` true, reasons `cannot measure` then `14: .team-ship/EVIDENCE.json: ... missing or unreadable`; `certified deny` receipts at seq 1, 3, 5; an `override gate=14` receipt written by the turn log and correctly reported unused, because stage 14's two final conditions are never overridable by design. Runs started with `gate_state.py start <pilot> ship hooks=observe hooks.stage=enforce`. Quoted in full in DENY-RUNS.md | DONE |
| 17 | Deny 2: Stop guard | brain, live session 2026-09-18 | no code change; the run is the evidence | `stop-checked` receipts seq 1 and 2 `block` with the remaining-stages reason, seq 3 `allow` with `reason=stop_hook_active is true: re-entry`; the model visibly restated the block to the owner instead of stopping. Started with `hooks=observe hooks.stop=enforce`; a first attempt with `--max-turns 4` produced only the block, so the recorded run used `--max-turns 10`. Quoted in full in DENY-RUNS.md | DONE |
| 18 | Deny 3: artifact-write ordering and Agent-spawn gates | brain, live session 2026-09-18 | no code change; the run is the evidence | Write of `.team-ship/PLAN.md` denied by `team-artifact-guard` with `cannot measure stage preconditions` then `missing: gates/handoff-1.json, gates/handoff-1-map.json, gates/1-arch-baseline.json, gates/handoff-2.json`, and the file did not exist afterwards; Agent spawn denied by `team-stage-guard` with `spawning Layer 2 build work before stage 6 is reachable`. Same run proves per-guard modes: two `stop-checked would-block` rows while artifact and stage enforced. Started with `hooks=observe hooks.artifact=enforce hooks.stage=enforce`. Quoted in full in DENY-RUNS.md | DONE |
| 19 | Pilot: /team:fix on a planted bug | Sonnet ran twice, brain classified, Sonnet verified run 2 from the run directory only | RUN 1 found five defects (PILOT-RUN-1.md); RUN 2 at e53b4ac PASS on every step and independently verified PASS (P1 to P10: every gate head, chain, receipt hashes by shasum, skip probe, evidence, red-green base, rerun of fix_gate and claim check, matrix, hooks registered, nothing contradicting) | MINORs: install.py --project writes $CLAUDE_PROJECT_DIR-relative hook paths that do not resolve in a consumer repository (task 36); parity is measured once at the baseline head; the brief's step 6 now names discover before run | DONE |
| 20 | Extend to develop, test, verify, reanalyse; delete covered prose | Sonnet builds one file per message, Sonnet verifies per file (every invocation executed on a scratch repository) | DONE: fix.md 60a3fd9 (+fef272e, 48a81a6), test.md 4f128c5 (+53f3ea9), develop.md b4fe7af, reanalyse.md 8b5655d, verify.md 1198ecd, ship.md 48a81a6 (three build rounds, fifty-seven rows, the whole pipeline executed to PASS_WITH_OVERRIDE by the verifier) | rulings R1 to R15 in TASK20-BRIEF.md; MINORs from ship.md's verification became tasks 38 and 39 | DONE |
| 21 | Rename the "Option B" label at team:ship.md:8 | brain | DONE 56184c9 | the header now reads "Approval model: one human approval gate after the plan, then the build and verification stages run without further prompts"; grep shows no other Option B in commands/team | DONE |
| 22 | Pull request to coco-research/coco | brain, owner approved 2026-09-18 ("Push and open the PR after the proof passes") | DONE: final proof 14/14 at 5350ab8; pushed with an audited GIT_GUARD_REASON (public OSS repository, owner-authorized, no internal or secret material); https://github.com/coco-research/coco/pull/196 | PR URL | DONE |
| 9b | prove_red follow-ups: fixture-aware runner (unittest setUp and tearDown, pytest fixtures), coverage attribution, renamed-module red classified valid, background-thread failures after return | grok builder, two rounds | adversarial verify: ceilings 1 and 3 PASS, ceiling 2 BLOCK (the TestCase path used debug(), which on this Python has no try/finally, so tearDown was skipped exactly when the test failed). Closing round fixed it and added a fixture that fails without the fix | Closed: a renamed implementation module classifies valid-red-missing-name (three rename attacks, including a rename outside the missing module, none fooled); setUp/setUpClass/tearDown/tearDownClass run, with tearDown in a finally so a failing test still logs it (unittest-teardown-on-fail, tearDown_log=2); a background thread that fails after return is a failure, and a thread still alive at the 1 second join is unrunnable, which is the honest verdict. 25/25 self-test cases. NOT closed, still TODO: the pytest fixture-aware runner and coverage attribution | DONE for three ceilings, OPEN for pytest fixtures and coverage |
| 23 | ripwire brownfield map as a Stage 1 machine artifact: brownfield_map.py, stub ripwire, nine self-test cases, README | Sonnet builds (rounds 1 and 2), Sonnet verifies (round 1 BLOCK on the repo-root cross-check, round 2 PASS: nested unrelated repository, linked worktree, subdirectory and symlinked root all behave; nothing written before the check; chain intact) | DONE 23ba310 | ripwire 0.6.1 built from source at ~/.local/bin/ripwire, audited offline; the manifest change is step 3 (TASK23-MANIFEST-BRIEF.md) and the --test-gate cross-check with prove_red is 23b, measured and recorded below | DONE (script 23ba310, manifest 9d89c36: stage 1 output and stage 2 input, minLines 12 of a measured 19, verified N1 to N7) |
| 24a | arch_gate.py run-aware: baseline (stage 1), plan-declare (stage 3), plan-verify and conformance (stage 13) with gate files and receipts; standalone mode unchanged | Sonnet builds (one round), Sonnet verifies PASS (A1 to A8: R2 proof reproduced by hand, every status, hand-written plans, receipt hashes by shasum, receipt corruption removes the gate file, nested repository refused) | DONE 6ec2d29 | MINORs: argv semantics differ between wrapper and child subcommands; _run_flags re-reads run.json; neither changed | DONE |
| 24b | ship_gate rows for gates/1-arch-baseline.json (present-only, informational), gates/3-arch-plan.json (stage 3), gates/13-arch-plan.json and gates/13-arch.json (stage 13, override "arch" at exit 1 or 2); four fixtures; real-pipeline case seeds the map | Sonnet builds (one round plus the map seed), Sonnet verifies PASS (S1 to S8: live fixtures, override semantics matched to the coverage row, head drift, stage queries, helper signatures) | DONE 02281f0 | MINOR 12d: one override covering two stage 13 files is listed twice in gates/14.json's overrides array, a pre-existing pattern shared with stage 11 | DONE |
| 24c | arch_gate cmd_conformance writes gates/13-arch.json with status and a receipt on every resolved path (NO_BASELINE, STALE, NOT_APPLICABLE, DISABLED, CURRENT) | Sonnet builds (one round), brain verifies mechanically (36 PASS lines, exit 0; conformance-not-applicable gate file quoted with status NOT_APPLICABLE) | DONE 49f4d20 | found by the 24b builder; verify-arch-gate A3 had not checked the file | DONE |
| 25 | Merge-honesty in claim_evidence.py: merge-claim findings backed by git ancestry, the stage 13 row, or a pr-opened receipt; fail closed on conditional sentences; fenced text scanned like plain text | Sonnet builds (one round), Sonnet verifies PASS (M1 to M6; MINOR: docstring lacked the fail-closed statement, added by the brain before commit) | DONE fb361e7 | 17 self-test cases; five fixtures with a local bare origin | DONE |
| 26 | Round bound: "rounds" row in ship_gate.py and fix_gate.py, blocks when build_rounds > 3, overridable | Haiku builds (two rounds: key and label fixed in round 2), Sonnet verifies PASS (R1 to R8; MINORs: rounds row computed rather than "not evaluated" under a broken chain, docstrings silent, the second fixed by the brain before commit) | DONE 071be23 | three fixtures each; boundary 0, 3, 4 checked live | DONE |
| 12c | Follow-up: under a broken chain the rounds row computes a value while the approval row says "not evaluated: chain broken"; make derived-from-receipts rows consistent when either aggregator is next opened | grok builder | adversarial verify BLOCK: both rows refused, but the reason strings were not equal because each row carries its own label prefix, and nothing asserted the agreement. Closing round added the assertion | Both rows now refuse under a broken chain and share the phrase `not evaluated: chain broken`, the approval row reading `Build: approval-receipt: not evaluated: chain broken` and the rounds row `not evaluated: chain broken`. New self-test case 12c-chain-broken-rows asserts both rows together, so a future edit cannot silently reintroduce a computing rounds row. fix_gate has no approval row, so it has no pair to keep consistent | DONE |
| 12d | Follow-up: gates/14.json lists one override once per covered file (twice for "arch" covering both stage 13 files, twice for "11"); list each override receipt once with the files it covered | grok builder | adversarial verify PASS | One entry per override receipt, carrying `files` with every path it covered: an `arch` override over both stage 13 files is one entry listing gates/13-arch-plan.json and gates/13-arch.json, and an `11` override over two files is one entry listing both. The fix_gate reader still resolves the new shape | DONE |
| 12e | Lesson, closed: 02281f0 changed ship_gate's required gates and hooks_selftest's seeded run stopped opening stage 2; caught by the clean-checkout proof at b4fe7af (13/14), fixed in 90b6200. Every verify brief for a change to ship_gate's required gates or the manifest now lists hooks_selftest.py | brain | DONE 90b6200 | run_fixtures.sh in a detached worktree is the proof that catches what a scoped round misses | DONE |
| 27 | Before-and-after comparator for reanalyse (two gate files at two commits) | TBD | deferred by ruling R7 | | TODO later |
| 28 | run_gate summary parser reads unittest output; shape field pytest, unittest or none; pytest wins; no-tests-collected only for a recognised shape | Sonnet builds (one round), Sonnet verifies PASS (U1 to U6; 25 cases; real unittest run parsed; old versus new code identical on a silent script) | DONE bdffc57 | MINOR: gates/8.json top-level parsed field is now a shape-none dict instead of null when the first command is not test-shaped; no reader | DONE |
| 29 | fix_gate stage 0 "map" row requiring gates/handoff-1-map.json; ancestor head rule for the map only; overridable by "map"; stage 1 query requires it | Haiku builds (two rounds), Sonnet verifies PASS (F1 to F7; ancestor rule probed with HEAD, an orphan branch, a descendant; stages 1 to 6 still strict) | DONE 652e2ed | MINORs: cmd_stage docstring fixed by the brain; a harmless no-op skip entry in two fixture builders left; the verifier overwrote pilot-repo.w33NNm's RUN pointer while probing, run 1's repository is no longer a faithful snapshot (its raw transcript is) | DONE |
| 30 | prove_red classify_red attributes the assertion to the first traceback frame inside the worktree; attribution_frame recorded; fixtures unittest-valid-red, unittest-invalid-red, helper-outside-worktree | Sonnet builds (one round), Sonnet verifies PASS (V1 to V5: pilot copy PASS with recheck 1, five adversarial shapes, regression, PYTHONPATH scoping, JSON compatibility) | DONE 88f5d90 | pilot finding 2 closed; MINOR: claim_evidence's hand-built mock test record lacks attribution_frame (cosmetic, its reader ignores it) | DONE |
| 31 | claim_evidence check resolves fields through entry, detail, detail.parsed, detail.commands[].summary, detail.coverage or total, recording the path; fixtures generated by running run_gate for real | Sonnet builds (one round), Sonnet verifies PASS (C1 to C7: pilot evidence backed, precedence with shadowed values, commands sum, differing shapes rejected) | DONE e53b4ac | pilot finding 5 closed | DONE |
| 32 | claim_evidence matrix maps requirements through their tests list to gates/9.json classes and gates/8.json; rows gain tests and route; UNVERIFIED checked before NOT MET | same commit as 31 | DONE e53b4ac | pilot finding 4 closed; verified with a real gates/9.json from the corrected prove_red | DONE |
| 33 | run_gate accepts --repo-root and --role after the subcommand as well as before; conflicting values exit 2 with one line | Haiku builds (one round, report pasted in full), brain verifies mechanically (25 self-test cases, six probes incl. the two conflict cases) | DONE cc715f9 | zsh trap for the brain: an unquoted variable is one word, so a probe loop must use sh -c or ${=var} | DONE |
| 34 | prove_red stubbed-implementation red for tests-only diffs (R13): stub every imported implementation module in the throwaway worktree, prove the new test fails against it; class valid-red-stub; mode recorded per test; five fixtures; the old never-red fixture gained an unrelated implementation touch to stay on red-green | Sonnet builds (one round), Sonnet verifies PASS (S1 to S8: six import shapes, stub fidelity, never-red cases, mixed diff, regression, fixtures byte-identical) | DONE ba00b26 | MINOR became task 35: the matrix's valid classes lacked valid-red-stub; deep relative imports beyond the package root fail in the runner for any mode (pre-existing) | DONE |
| 35 | claim_evidence _VALID_RED_CLASSES gains valid-red-stub; fixture matrix-tests-met-stub | Haiku builds (one round, report pasted in full), brain verifies mechanically (25 cases OK; main worktree confirmed untouched after the report's first git status came from the wrong directory) | DONE 688b1fa | | DONE |
| 36 | install.py --project writes absolute hook paths when the hooks directory is not inside the target project; --check requires the referenced files to exist; two self-test cases | Haiku builds (one round, complete report), brain verifies mechanically (50 cases OK, live consumer install, hook run on empty stdin, removal) | DONE c469e98 | MINOR: --check reports a dangling registration as event/hook_file rather than by the missing path | DONE |
| 37 | ship_gate head rule scoped by stage (R15): pre-build gates accept an ancestor head, stages 7 to 14 strict; head_rule per row; three fixtures | Sonnet builds (one round), Sonnet verifies PASS (H1 to H6: whole ship pipeline by hand to PASS_WITH_OVERRIDE with no post-build rerun, foreign head, ancestor head on a built-tree gate, a descendant commit from a clone, stage query, helper signatures, ship.md sentence) | DONE 45e5dd2 | MINOR: hand-editing a gate file in place breaks the chain's own sha check, so probes must rewrite the receipt too, which is the tamper evidence working | DONE |
| 38 | ship_gate stage 1 requires gates/handoff-1-map.json (pre-build head rule, override "map"); hooks_selftest seeds it | Haiku built the code and fixtures but left the two new cases out of the self-test list and reported fix_gate's line as ship_gate's; the brain wired the cases, fixed a duplicate fixture-copy collision, and ran all three suites (ship_gate 54 lines, fix_gate 25, hooks 50) | DONE 9f41d29 | the Haiku builder never answered which git commands it ran while task 39's edit vanished; the loss was repaired from the pasted diff | DONE |
| 39 | render_pr_body reads detail.instruction (fallback reason) and renders every override receipt in order, one fence each; fixtures in the real shape plus old-shape and two-gate cases | Haiku builds (two rounds), brain reapplied the change from the pasted diff after it vanished from the shared worktree, added an order check and fixed the per-case OK line, five cases OK | DONE 5b76cb6 | verify-prose-ship MINOR 5 closed; the loss is under investigation with the task 38 builder | DONE |
| 5f | Follow-up: gate_state.py verify-chain and status take no --repo-root and resolve from cwd, unlike every other script | grok builder | brain mechanical check: 37/37 self-test cases, both subcommands exercised from a foreign cwd, one sys.exit in the main guard, no em dash, no section sign | status and verify-chain take --repo-root, defaulting to the current directory so no existing caller changes behaviour. New cases verify-chain-repo-root and status-repo-root | DONE |

| 23b | ripwire `--test-gate` cross-checked against prove_red's enumeration on the pilot | grok builder, measured | adversarial verify PASS on the cause, with one understatement corrected | DISAGREEMENT, and it is a real one: on one committed bugfix, `prove_red enumerate` named the one new function (test_calc.py::test_clamp_high), while ripwire 0.6.1 `--test-gate` with no arguments listed nothing (changed=0 tests=0, because its default input is the working-tree diff, clean after a commit) and `--test-gate=calc.py,test_calc.py` listed the whole file (changed=2 tests=1) rather than the function. Cause: selection scope, whole file versus the new function, plus the default input. Full payloads, verbatim commands and the corrected `at=` detail in TASK23B-CROSSCHECK.md | DONE (measured; the disagreement is the finding) |

## Decisions log

- 2026-09-17: Roster fixed by the owner. Fable 5.1 brain, Haiku 4.5 builder, Sonnet 5 verifier, Opus 5 only with permission. Test fan-outs are Haiku only.
- 2026-09-17: Probe hook is observe-only and exits 0 unconditionally with a 2s stdin timeout. That timeout is correct for a probe and would be wrong for a blocking hook.
- 2026-09-17: PreToolUse and PostToolUse probe entries omit the matcher field so they observe every tool, rather than relying on an unverified wildcard syntax.
- 2026-09-17: Bash PostToolUse tool_response on this build carries stdout, stderr, interrupted, isImage, noOutputExpected and no exit code. Confirms the plan's rule: the runner writes its own exit codes to gates/*.json; no hook infers an exit code from tool_response.
- 2026-09-17: Hooks receive permission_mode. Receipts will record it. This session runs in auto mode and builders inherit it, so the bypassPermissions deny test (assumption 1) cannot be executed from this session and remains documentation-confirmed only. It is recorded as such, not claimed as tested.
- 2026-09-17: A hook registered after a subagent launched still fired for that subagent's calls, so hook configuration is read per call rather than cached at agent start on this build.
- 2026-09-17: SubagentStop delivers agent_transcript_path and last_assistant_message. The agent-returned receipt will be written by the SubagentStop hook from the subagent's own transcript, not recorded by the orchestrator. This replaces a model-written receipt with a harness-written one.
- 2026-09-17: Stop delivers background_tasks and stop_hook_active. The Stop guard must return ok true while background_tasks is non-empty, otherwise it would force the orchestrator to continue while builders are still running, and it uses stop_hook_active to avoid re-entering itself.
- 2026-09-17: The harness fails open on hook timeout (test 2). PLAN section 4 amended: gates are cheap-only with a 60s timeout, every gating PreToolUse hook appends a certified receipt keyed by tool_use_id, and a paired PostToolUse hook appends a gate-timeout block receipt when that certification is absent. Fail-closed is achieved at the run level (cannot certify) rather than the call level (cannot proceed). Two receipt kinds added to the closed set: certified, gate-timeout. The gate_state fix brief must be extended with these two kinds before Sonnet re-verifies.
- 2026-09-17: The gate_state builder committed 43c2627 on its own despite "no commit", sweeping in about forty unverified files from six tasks plus an embedded-repository gitlink. Brain applied `git reset f3f028c` (mixed, working tree untouched, nothing pushed). Rule from here: after every builder return, run git log -1 and git status before anything else; builders are reminded in every brief that the brain alone commits. The .gitignore _build glob was widened to fixtures/**/_build/ because one builder nested _build two levels deep.
- 2026-09-17: Briefing lesson. The arch_gate builder took 181 tool uses and 33 minutes on a fixture restructure because the brief said "do not guess the fix, read how the real tool pins" when the brain already knew the convention from architecture.md. Haiku is cheap per token and expensive per discovery. Future briefs state every fact the brain already holds and reserve "go read it" for facts the brain does not have. The gate_state builder, given prescriptive briefs, averaged 26 to 50 tool uses per round.
- 2026-09-17: Before launching wave 2, the brain adds one glob line to the worktree .gitignore, skills/team-gate/scripts/fixtures/*/_build/, so five parallel builders do not race on the same file. Deferred until the arch_gate verifier finishes, because that verifier is checking .gitignore right now.
- 2026-09-17: UserPromptSubmit delivers the raw prompt. team-turn-log.js hashes it and writes an approval receipt when the text matches the approval pattern. Approval provenance (Opus S5-B) is therefore mechanical, not structural-only as the plan assumed.
- 2026-09-17 evening: wave-2 verdicts recorded (7, 8, 10, 11 BLOCK; 9 stub; 5 and 6 DONE and committed). Rulings added to PLAN section 4: artifacts in .team-ship/, gates only in the run directory; one renderer for EVIDENCE.md with a deterministic header on line 1; ship-manifest uses ship.md's fourteen stage numbers with the approval gate keyed "approval"; discover writes 7-discover.json; fixture generators anchor on __file__, accept --out, pin git dates from TEAM_FIXED_TS. Brain recommends Sonnet builders for the rebuild of 7, 8, 9, 11; owner decision pending.
- 2026-09-18: Owner approved Sonnet builders for the rebuild of tasks 7, 8, 9, 11 (roster amendment recorded in HANDOFF section 1). Four Sonnet builders launched in parallel.
