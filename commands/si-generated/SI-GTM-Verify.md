# /SI-GTM-Verify — re-check a prior output's citations (mitigation #6)

> Maintenance verb of the Sales, GTM & Marketing Super Intelligence Team. Generated by `build_commands.py`.

Expects the last output (or `--output <path>`). For every attributed stance in the output, check it traces to the named persona's `public_stances` / `v2_panel_attribution` / `recent_signal_12mo`. Emit a table: claim / persona / FOUND (evidence_url) | EXTRAPOLATED | CONTRADICTED. Summary grounding score.


ARGUMENTS: {{ARGUMENTS}}
