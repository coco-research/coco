# Task 20 brief: extend the gates to develop, test, verify, reanalyse and fix, then delete the prose the scripts now cover

Source of truth for the gaps: TASK20-INVENTORY.md (one hundred instruction rows across six command files: 39 COVERED, 41 PARTIAL, 11 JUDGEMENT, 9 UNCOVERED). This brief records the brain's rulings on the conflicts and gaps the inventory found, splits the work into three script tasks that land first (24, 25, 26) and one prose task (20 proper) that lands last, and fixes the verification each one gets. The opening line of every builder message is "You never run git commit, git add, or git push; the brain commits." Every rule from WAVE2-REBUILD-ADDENDUM.md applies to every script change, including the smoke check and the temporary-copy self-test.

## Rulings

R1. The exit contract wins over prose that names a different verdict. ship.md:226 says a validator that cannot be executed is a BLOCK; arch_gate.py exits 2. Exit 2 is UNVERIFIED, and ship_gate treats UNVERIFIED as blocking the pull request, so the prose's outcome (no PR) is preserved while the three-valued contract stays uniform. The prose is rewritten to say UNVERIFIED, which blocks.

R2. Currency is judged at Stage 1, as ship.md:228-234 says, and arch_gate.py changes to match. arch_gate.py's present rule, "index pinned to commit != HEAD: UNVERIFIED", is right for a standalone invocation and wrong inside a ship run, where Stage 6 has committed and the pin is expected to be behind HEAD. Inside a run, arch_gate compares the pin to the HEAD recorded at Stage 1 in gates/1-arch-baseline.json; equal means CURRENT and the two scripts run with drift attributable to this build; unequal means the pin was already stale at Stage 1 and the outcome is UNVERIFIED. Outside a run (no gates/1-arch-baseline.json) the present rule stands unchanged.

R3. NOT APPLICABLE and DISABLED follow the coverage precedent. A repository with no .arch/index.json gives arch status NOT_APPLICABLE; a run started with flags no_arch gives DISABLED. Both are exit 2 with the status in the gate file, and ship_gate's arch row blocks unless an override receipt names gate "arch", exactly as coverage NOT_APPLICABLE blocks unless an override names "coverage". The human records the bypass in their own words and the PR body carries it. One rule to learn, no special case in the aggregator. The owner can relax this with a one-line ruling later.

R4. Provisioning integration dependencies and exporting a DSN stay a model action. No script provisions anything. The honest degrade ship.md:134 asks for is recorded as an override receipt naming gate "integration" with the reason, which ship_gate lists and render_pr_body prints. run_gate already measures the consequence: a skipped test blocks.

R5. The three-round bound becomes mechanical. gate_state.derive_status already counts build_rounds. ship_gate and fix_gate gain a "rounds" row that blocks when build_rounds exceeds 3; it is overridable ("override rounds: <reason>") because a human may sanction one more round in their own words.

R6. Merge-honesty becomes mechanical. claim_evidence check gains a merge-claim class: the phrases "merged", "is merged", "ci green", "ci is green", "shipped", "deployed", "landed on main" (in the lexicon as multi-word entries) are findings of type merge-claim unless the condition holds: "merged" and "landed on main" require `git merge-base --is-ancestor HEAD origin/main` true in the repository root; "ci green" requires the stage 13 row of gates/14.json to pass; "shipped" and "deployed" require a pr-opened receipt. Number words remain unverifiable as before.

R7. The before-and-after comparator reanalyse needs (rows 44 and 54) is deferred to a follow-up task 27 and recorded in the register; reanalyse gets the arch_gate and run_gate invocations now and the model quotes the two gate files it has.

R8. PARTIAL and EXCEEDED grades in verify.md stay model judgement written in prose after the machine matrix; the matrix has three grades and that ruling stands.

R9. The command files invoke scripts by the installed path ~/.claude/skills/team-gate/scripts/<script>, matching PLAN.md section 6 and the arch-index arrangement. The symlink ~/.claude/skills/team-gate does not exist yet; the brain asks the owner before creating it, after the branch merges. Until then the pilot uses the worktree path.

## Task 24: arch_gate.py becomes run-aware and gains the built-as-declared gate

Owner: Sonnet builder. Verifier: Sonnet, scoped, with the round-3 arch_gate regression list plus the new cases.

Shape. arch_gate.py keeps its present flag-only invocation for standalone use and gains three subcommands that require an active run (gate_state.find_run, absence exit 2):

1. `baseline --repo-root <r>`: Stage 1. Records {pin, head, status} where status is DISABLED when run.json flags carry no_arch, NOT_APPLICABLE when .arch/index.json is absent, STALE when the pin is not HEAD, CURRENT otherwise. Writes gates/1-arch-baseline.json (argv, cwd, head, exit, summary, pin, status) with a gate-result receipt. Exit 0 for CURRENT, 2 for the other three (R3), never 1.
2. `plan-declare --repo-root <r>`: Stage 3. Runs verify_arch_plan.py .team-ship/ARCH-PLAN.json --declare --repo-root <r> from the resolved tools directory (resolve_tools_dir gains verify_arch_plan.py in its required list). Writes gates/3-arch-plan.json with the child's exit, stdout tail and summary; exit 0 pass, 1 when the child exits non-zero (ship.md:73), 2 when the child cannot run or ARCH-PLAN.json is absent.
3. `plan-verify --repo-root <r>`: Stage 13. Same wrapper with --verify; writes gates/13-arch-plan.json; a component declared new or modified whose path is missing, or a file under an outOfScope path, is what the child reports as failure and maps to exit 1; unrunnable or unresolvable declaredAtCommit is exit 2 (ship.md:181-186).
4. `conformance --repo-root <r>`: Stage 13. The present conformance logic, with R2 applied: read gates/1-arch-baseline.json; DISABLED or NOT_APPLICABLE or STALE reproduce that status with exit 2; CURRENT runs validate_index.py and arch_drift.py with the present pass rules minus the pin-vs-HEAD check. Writes gates/13-arch.json plus the existing .arch/ARCH-GATE.json.

Every gate file follows rule 3 of the addendum and gets a gate-result receipt; the tools_dir field stays. The self-test grows by: baseline-current (0), baseline-stale (2, "STALE"), baseline-no-index (2, "NOT_APPLICABLE"), baseline-disabled (2, "DISABLED"), declare-pass (0), declare-fail (1, the child's reason), declare-no-plan (2), verify-pass (0), verify-missing-path (1, names the component), conformance-current-after-commit (0: the pin is behind HEAD because a fixture commit followed the baseline, and the outcome is still PASS, which is the R2 proof), conformance-stale-baseline (2, "UNVERIFIED"), no-run (2). Fixtures need verify_arch_plan.py; its location and the ARCH-PLAN.json shape come from skills/arch-index and commands/team/ship.md:51-61.

ship_gate.py, scoped follow-on in the same task: stage 3's required gates gain gates/3-arch-plan.json; stage 13 gains gates/13-arch-plan.json and gates/13-arch.json, with the R3 override names "arch" for both; stage 1 gains gates/1-arch-baseline.json as present-but-not-blocking (its exit 2 statuses are informational at stage 1 and are enforced at stage 13). Fixtures all-green and the stage-query cases gain the new files; two new fixtures: arch-not-applicable-blocks (1, names "arch") and arch-not-applicable-overridden (0, PASS_WITH_OVERRIDE). fix_gate self-test must still pass (both aggregators' self-tests run first in any verification of either).

ship-manifest.json: no change; the arch evidence lives in gate files under the run directory, which is where the aggregator reads, and .arch/ARCH-GATE.json stays as the committed human-facing record. ship.md's mention of .team-ship/ARCH-PLAN-EVIDENCE.md and .arch/ARCH-EVIDENCE.md is replaced in task 20 by the gate file names.

## Task 25: merge-honesty in claim_evidence.py

Owner: Sonnet builder. Verifier: Sonnet, scoped. Implements R6. claim-lexicon.json gains a "mergeClaims" object mapping each phrase to its condition name (ancestor-of-main, ci-mirror-pass, pr-opened). claim_evidence check evaluates the condition once per run (git merge-base with a 10-second timeout, exit 2 when git cannot answer; gates/14.json stage 13 row; receipts.jsonl kind pr-opened) and records each merge claim in gates/12.json with type merge-claim, the condition and its measured value. Fixtures: merged-not-ancestor (1, "merge-claim"), merged-ancestor (0, fixture repository where HEAD is on origin/main via a local bare remote), ci-green-without-stage-13 (1), shipped-without-pr (1), shipped-with-pr-receipt (0). The lexicon's single-entry rule for multi-word phrases stands.

## Task 26: the round bound in ship_gate.py and fix_gate.py

Owner: Haiku builder, because the change is mechanical and fully specified; verifier: Sonnet, scoped. Implements R5. Both aggregators add a "rounds" row: required, present, exit 1 when derive_status build_rounds > 3 with reason "build_rounds <n> exceeds 3", overridable by gate name "rounds". Fixtures: rounds-exceeded (1, "exceeds 3"), rounds-overridden (0, PASS_WITH_OVERRIDE), rounds-three (0). The row is listed in gates/14.json and gates/fix.json like any other.

## Task 20 proper: the prose

Owner: Sonnet builder, one file per message, in the order fix, test, develop, verify, reanalyse, ship, so the smallest file establishes the style. Verifier: Sonnet, one round per file, checking three things: every deleted paragraph maps to a COVERED row of TASK20-INVENTORY.md or to a PARTIAL row whose remainder survives as one sentence; every invocation names an existing script and subcommand whose docstring writes the gate file the prose then quotes; every JUDGEMENT row is intact. Lands only after 24, 25 and 26 are committed, because the UNCOVERED rows become invocations of those scripts.

Style of the replacement. Each gate in a command file becomes a block of at most four lines: the invocation with the installed path (R9), then "exit 0: continue and quote the gate file's summary", "exit 1: BLOCK; quote the reasons from the gate file and stop or loop back", "exit 2: UNVERIFIED; the pull request cannot open until the gate passes or an override receipt names the gate". Nothing about how the script works is repeated in prose; the script's docstring is the reference. Approval stays a human turn: the command asks the question and says that the approval receipt is written by the harness from the human's words, never by the model. Rows the inventory marks JUDGEMENT keep their prose verbatim. The "max 3 rounds" and merge-honesty sentences become one line each pointing at their rows.

Provisioning (R4) keeps one paragraph per file: provision what CI provisions; when something cannot be provisioned, record "override integration: <reason>" in your own words as the human, and run_gate's skip rule decides the rest.

Verification for each file, in addition to the verifier's three checks: `python3 <installed or worktree path> --help` exits 0 for every invoked script, run from /tmp; `grep -c "team-gate/scripts"` equals the number of gate blocks the inventory predicts for that file; no em dash, no section sign; the file's frontmatter description is unchanged unless the stage count changed.

## Order

1. Task 26 (Haiku), the smallest, while 24 is built.
2. Task 24 (Sonnet), arch_gate then ship_gate, two scoped verify rounds.
3. Task 25 (Sonnet).
4. run_fixtures.sh unchanged in script list (arch_gate, claim_evidence, ship_gate, fix_gate are already in it); clean-checkout proof rerun after 24 to 26.
5. Task 20 proper, six files, six scoped verify rounds.
6. Register rows 24, 25, 26, 27 added; PLAN.md section 4 carries R1 to R9.

R10 (added after fix.md round 1). The brownfield map is the first gate block of every pipeline file, before the model reads any code, whatever the inventory rows say, because the owner directed that the map is the first step on every existing repository. The em dash rule applies to every line of a file we edit, including titles and JUDGEMENT prose; replacing an em dash with a colon or comma is punctuation, not content. Commands inside a gate block are one per line, never chained with `&&`, so each exit code is read on its own.

R11 (added after fix.md round 2). A gate block certifies a step; it does not replace the sentence that tells the model to perform the step. Every pipeline file keeps a numbered sequence of the actions the model takes and the artifacts it writes (which file, by which layer, in what order), with each gate block placed after the action it measures. The per-file verifier gains a fourth check: the sequence of actions and artifacts is stated and matches the gate order of the aggregator's docstring.

R12 (from the pilot). run_gate.py run refuses a discover file recorded at a different HEAD (exit 2, "run discover again"), so every pipeline file runs discover then run after each commit, as one block of two lines. run_gate.py's --repo-root and --role are parent-level flags and are written before the subcommand (task 33 makes both positions valid, but the files use the form that is valid today). When prove_red.py prove needs --base, the step that records the baseline head says so, because gates/8.json is rewritten by the later run.

R13 (from the test.md build). prove_red's red-green proof reverts the implementation paths in the diff between --base and HEAD, so a tests-only change has nothing to revert and every new test reads never-red. The mechanical proof for a tests-only change is a stubbed-implementation red: task 34 makes prove, when the enumerated diff carries no implementation paths, replace in the throwaway worktree every implementation module the new test file imports (resolved by AST import scan to worktree paths whose classify_path is implementation) with a stub whose top-level names raise NotImplementedError, and prove that the test fails against the stub (class valid-red-stub, attribution the test frame or the stub frame); a test that still passes is never-red with the reason "exercises no implementation module"; a test that imports no implementation module is never-red with that reason. gates/9.json records mode red-green or stub per test. Until 34 lands, test.md wires prove and recheck as row 36 says and carries one sentence naming the interim override "override red-green: tests-only change, stub proof pending task 34", to be removed when 34 is committed.

R14 (from the develop.md build). The develop pipeline writes implementation and tests together, so the red-green proof applies to its own build even though the inventory found no TDD row in develop.md: a baseline discover and run before Layer 2 records the base head, and the Regression Tests block runs prove_red prove at that base and recheck before evidence. The ship pipeline's stage 9 reruns the same proof over the whole build; the two are the same gate at two scopes.

R15 (from the ship.md build). ship_gate's head rule was strict equality for every gate file, but stages 1 to 6 and the approval gate run before the build, and Stage 6's commits move HEAD, so every pre-build gate file drifted and the only way to pass was to rerun the stage checks after the build, which measures nothing new. Ruling: the head rule is scoped to when a gate runs. Gate files of stages 1 to 6 (gates/handoff-1.json to handoff-6.json, handoff-approval.json, 1-arch-baseline.json, 3-arch-plan.json) satisfy the head check when their recorded head is HEAD or an ancestor of HEAD, computed with git merge-base --is-ancestor as fix_gate's map row already does; gate files of stages 7 to 14, EVIDENCE.json and the stage 13 files keep strict equality because they measure the built tree. A recorded head that is not an ancestor is still drift and still not overridable. ship.md must not tell the model to rerun the stage checks after the build.
