{
  "schemaVersion": "1.0",
  "declaredAt": "2026-08-02T02:00:00Z",
  "declaredAtCommit": "PLACEHOLDER",
  "basis": "new",
  "components": [
    {
      "id": "skill-library",
      "title": "Agent Skill Library",
      "purpose": "Holds reusable agent capabilities as self-contained skill directories.",
      "status": "modified",
      "connections": ["webhook-ingest"],
      "intendedPaths": {
        "directories": ["skills"],
        "files": [],
        "rationale": "Existing tree; declared so the plan is complete."
      }
    },
    {
      "id": "webhook-ingest",
      "title": "Webhook Ingestion Service",
      "purpose": "Receives inbound webhooks and dispatches them to handlers. This component is declared but deliberately never built, which is the case this fixture exists to catch.",
      "status": "new",
      "connections": ["skill-library"],
      "intendedPaths": {
        "directories": ["services/webhooks"],
        "files": ["services/webhooks/handler.py"],
        "rationale": "Declared location for the new service; nothing is ever written here."
      }
    }
  ],
  "outOfScope": [],
  "notes": "Fixture: exercises the central check. A component is declared new, the build never produces it, and --verify must catch both declared paths as missing. This is the failure the whole stage exists to detect."
}
